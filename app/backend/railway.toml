# Railway service config for the backend service (rootDirectory = app/backend).
# Pins the build to the Dockerfile builder so Railway never falls back to Railpack auto-detect.
# watchPatterns limits auto-deploys to changes under this service's directory.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["app/backend/**"]
