# Railway service config for the frontend service (rootDirectory = app/frontend).
# Pins the build to the Dockerfile builder so the setup is reproducible without dashboard settings.
# watchPatterns limits auto-deploys to changes under this service's directory.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["app/frontend/**"]
